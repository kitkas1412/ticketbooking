<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <!-- POM gốc quản lý cấu hình và danh sách module của dự án. -->
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath /> <!-- Tìm POM cha trong kho Maven. -->
    </parent>

    <groupId>me.kitkas1412</groupId>
    <artifactId>ticketbooking</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <spring-boot.version>4.0.7</spring-boot.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>3.1.1</springdoc.version>
        <jacoco.version>0.8.15</jacoco.version>
    </properties>

    <!-- Khai báo TẤT CẢ module con — thứ tự không quan trọng,
         Maven reactor tự tính toán thứ tự build dựa trên dependency -->
    <modules>
        <module>start</module>
        <module>common</module>
        <module>infrastructure</module>
        <module>modules/event</module>
        <module>modules/user</module>
        <module>modules/order</module>
        <module>modules/orderitem</module>
        <module>modules/ticket</module>
        <module>modules/outboxevent</module>
    </modules>

    <!-- Dependency test dùng chung: mọi module con kế thừa JUnit 5, Mockito, AssertJ và spring-test. -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Đo độ phủ test: mỗi module sinh báo cáo tại target/site/jacoco khi chạy `mvn verify`. -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <name>ticketbooking</name>
    <description>ticketbooking</description>
    <url />
    <licenses>
        <license />
    </licenses>
    <developers>
        <developer />
    </developers>
    <scm>
        <connection />
        <developerConnection />
        <tag />
        <url />
    </scm>

</project>